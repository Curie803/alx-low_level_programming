#include "main.h"
/**
 * _abs - return s the absolute value
 * @value: value to be returned
 * Return: 0 if successful
 */

int _abs(int value)
{
	if (value < 0)
	{
		return (-(value));
	}
	else
	{
		return (value);
	}
}

